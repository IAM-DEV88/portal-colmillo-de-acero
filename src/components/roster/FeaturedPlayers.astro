---
// Definir los tipos de datos
interface ClassInfo {
  color: string;
  name: string;
}

interface Recognition {
  title: string;
  description: string;
  icon: string;
}

interface FeaturedPlayer {
  playerName: string;
  recognitionIndices: number[];
}

interface RosterMember {
  name: string;
  class: string;
  rank?: string;
  gearScore?: number;
  [key: string]: any;
}

interface Props {
  featuredPlayers: any[]; // Temporary any type to handle both old and new formats
  rosterData: RosterMember[];
  recognitions?: Recognition[]; // Add recognitions prop
  currentPage?: number;
  totalPages?: number;
}

const { featuredPlayers = [], rosterData = [], recognitions = [] } = Astro.props as Props;

// Definir la información de clases por defecto
const defaultClassInfo: Record<string, ClassInfo> = {
  Guerrero: { color: 'C79C6E', name: 'Guerrero' },
  Paladín: { color: 'F58CBA', name: 'Paladín' },
  Cazador: { color: 'ABD473', name: 'Cazador' },
  Pícaro: { color: 'FFF569', name: 'Pícaro' },
  Sacerdote: { color: 'FFFFFF', name: 'Sacerdote' },
  'Caballero de la Muerte': { color: 'C41F3B', name: 'Caballero de la Muerte' },
  Chamán: { color: '0070DE', name: 'Chamán' },
  Mago: { color: '69CCF0', name: 'Mago' },
  Brujo: { color: '9482C9', name: 'Brujo' },
  Druida: { color: 'FF7D0A', name: 'Druida' },
};

// Función para obtener los datos del jugador del roster
const getPlayerData = (playerName: string): RosterMember | undefined => {
  return rosterData.find(
    (member) => member.name.toLowerCase() === playerName.toLowerCase()
  );
};

// Si no hay jugadores destacados, no mostrar la sección
if (!featuredPlayers.length) return null;

// Obtener las props de paginación
const { currentPage = 1, totalPages = 1 } = Astro.props;
const itemsPerPage = 6;

// Función para obtener los jugadores de la página actual
const getPaginatedPlayers = () => {
  const startIndex = (currentPage - 1) * itemsPerPage;
  return featuredPlayers.slice(startIndex, startIndex + itemsPerPage);
};

// Función para actualizar la visualización de jugadores
const updateFeaturedPlayers = () => {
  const container = document.getElementById('featured-players-container');
  const pagination = document.getElementById('featured-players-pagination');
  
  if (!container || !pagination) return;
  
  // Actualizar jugadores
  const playersToShow = getPaginatedPlayers();
  container.innerHTML = playersToShow.map(player => renderPlayerCard(player)).join('');
  
  // Actualizar controles de paginación
  updatePaginationControls();
};

// Función para renderizar un jugador
const renderPlayerCard = (player: any) => {
  const playerName = getPlayerName(player);
  const playerData = getPlayerData(playerName);
  if (!playerData) return '';
  
  const classData = {
    color: playerData.class ? (defaultClassInfo[playerData.class]?.color || 'FFFFFF') : 'FFFFFF',
    name: playerData.class || 'Desconocido'
  };
  
  const recognitionText = getRecognitionText(player);
  
  return `
    <div class="bg-gray-900/60 backdrop-blur-sm border border-amber-900/40 rounded-lg p-4 hover:border-amber-700/60 transition-all duration-300 transform hover:scale-[1.02] hover:shadow-lg">
      <div class="flex items-center space-x-4">
        <div class="flex-1 min-w-0">
          <div class="flex gap-2">
            <div class="w-8 h-8 self-center">
              <img 
                src="/images/avatars/class_${playerData.class || 'default'}.jpg"
                alt="${playerData.class || 'Clase'}"
                class="w-full h-full rounded-full border-2 border-amber-600/50 object-cover"
                onerror="this.src='/images/avatars/default.png'"
              />
            </div>
            <h3 class="text-lg font-semibold text-amber-100 truncate" style="color: #${classData.color}">
              ${playerName}
            </h3>
          </div>
          ${playerData.rank ? `
            <div class="bg-amber-700 text-amber-100 text-xs inline-block font-bold px-2 py-0.5 rounded-full border border-amber-600/50 mt-1">
              ${playerData.rank}
            </div>
          ` : ''}
          <p class="text-sm text-amber-100/80 mt-2">
            ${recognitionText}
          </p>
          ${player.recognitionIndices?.length > 0 ? `
            <div class="mt-2 space-y-1">
              ${player.recognitionIndices.map((idx: number) => {
                const recog = recognitions[idx];
                if (!recog) return '';
                return `
                  <div class="text-xs text-amber-100/70 flex items-start gap-1">
                    <span class="text-amber-300/80">•</span>
                    <span>${recog.description}</span>
                  </div>
                `;
              }).join('')}
            </div>
          ` : ''}
        </div>
      </div>
    </div>
  `;
};

// Función para actualizar los controles de paginación
const updatePaginationControls = () => {
  const pagination = document.getElementById('featured-players-pagination');
  if (!pagination) return;
  
  let paginationHTML = `
    <div class="flex items-center justify-center gap-2 mt-6">
      <button 
        onclick="changePage(${currentPage - 1})" 
        ${currentPage === 1 ? 'disabled' : ''}
        class="px-3 py-1 rounded-md ${currentPage === 1 ? 'bg-gray-700 text-gray-500 cursor-not-allowed' : 'bg-amber-700/50 hover:bg-amber-600/60 text-amber-100'}"
      >
        Anterior
      </button>
      <div class="flex items-center gap-1">
  `;
  
  // Mostrar máximo 5 números de página
  let startPage = Math.max(1, currentPage - 2);
  let endPage = Math.min(totalPages, startPage + 4);
  
  if (endPage - startPage < 4) {
    startPage = Math.max(1, endPage - 4);
  }
  
  if (startPage > 1) {
    paginationHTML += `
      <button 
        onclick="changePage(1)" 
        class="w-8 h-8 rounded-md ${1 === currentPage ? 'bg-amber-600 text-white' : 'bg-gray-700 hover:bg-gray-600 text-gray-200'}"
      >
        1
      </button>
      ${startPage > 2 ? '<span class="px-2">...</span>' : ''}
    `;
  }
  
  for (let i = startPage; i <= endPage; i++) {
    paginationHTML += `
      <button 
        onclick="changePage(${i})" 
        class="w-8 h-8 rounded-md ${i === currentPage ? 'bg-amber-600 text-white' : 'bg-gray-700 hover:bg-gray-600 text-gray-200'}"
      >
        ${i}
      </button>
    `;
  }
  
  if (endPage < totalPages) {
    paginationHTML += `
      ${endPage < totalPages - 1 ? '<span class="px-2">...</span>' : ''}
      <button 
        onclick="changePage(${totalPages})" 
        class="w-8 h-8 rounded-md ${totalPages === currentPage ? 'bg-amber-600 text-white' : 'bg-gray-700 hover:bg-gray-600 text-gray-200'}"
      >
        ${totalPages}
      </button>
    `;
  }
  
  paginationHTML += `
      </div>
      <button 
        onclick="changePage(${currentPage + 1})" 
        ${currentPage === totalPages ? 'disabled' : ''}
        class="px-3 py-1 rounded-md ${currentPage === totalPages ? 'bg-gray-700 text-gray-500 cursor-not-allowed' : 'bg-amber-700/50 hover:bg-amber-600/60 text-amber-100'}"
      >
        Siguiente
      </button>
    </div>
  `;
  
  pagination.innerHTML = paginationHTML;
};

// Función para obtener el texto de reconocimiento
const getRecognitionText = (player: any): string => {
  // Si ya tiene la propiedad recognition (formato antiguo), usarla
  if ('recognition' in player) {
    return player.recognition;
  }
  
  // Si tiene recognitionIndices, obtener los reconocimientos del array central
  if (player.recognitionIndices && player.recognitionIndices.length > 0) {
    const recogs = player.recognitionIndices
      .map((idx: number) => recognitions[idx]?.title || '')
      .filter(Boolean);
    return recogs.join(' • ');
  }
  
  return 'Reconocimiento especial';
};

// Función para obtener el nombre del jugador
const getPlayerName = (player: any): string => {
  return player.playerName || player.name || '';
};

// Inicializar la página desde la URL
if (typeof window !== 'undefined') {
  const urlParams = new URLSearchParams(window.location.search);
  const page = parseInt(urlParams.get('page') || '1');
  if (page >= 1 && page <= totalPages) {
    currentPage = page;
  }
  
  // Asegurarse de que las funciones estén disponibles globalmente
  window.updateFeaturedPlayers = updateFeaturedPlayers;
  window.changePage = changePage;
  window.currentPage = currentPage;
  window.totalPages = totalPages;
}
---

<div class="mb-8 bg-gradient-to-r from-amber-900/30 to-amber-800/20 backdrop-blur-sm border border-amber-800/40 rounded-xl p-6 shadow-lg">
  <div class="flex justify-between items-center mb-6">
    <h2 class="text-2xl font-bold text-amber-200/90 uppercase tracking-wider">
      Jugadores Destacados del Mes
    </h2>
    <a 
      href="/hall-of-fame" 
      class="text-amber-200/90 hover:text-amber-200/80 text-sm font-medium flex items-center gap-1 transition-colors"
      title="Ver todos los jugadores destacados"
    >
      Ver todos
      <svg xmlns="http://www.w3.org/2000/svg" class="h-4 w-4" viewBox="0 0 20 20" fill="currentColor">
        <path fill-rule="evenodd" d="M7.293 14.707a1 1 0 010-1.414L10.586 10 7.293 6.707a1 1 0 011.414-1.414l4 4a1 1 0 010 1.414l-4 4a1 1 0 01-1.414 0z" clip-rule="evenodd" />
      </svg>
    </a>
  </div>
  <div id="featured-players-container" class="grid grid-cols-1 sm:grid-cols-2 lg:grid-cols-3 gap-6">
    {getPaginatedPlayers().map((player, index) => {
      const playerName = getPlayerName(player);
      const playerData = getPlayerData(playerName);
      if (!playerData) return null;
      
      const classData = {
        color: playerData.class ? (defaultClassInfo[playerData.class]?.color || 'FFFFFF') : 'FFFFFF',
        name: playerData.class || 'Desconocido'
      };
      
      const recognitionText = getRecognitionText(player);
      
      return (
        <div class="bg-gray-900/60 backdrop-blur-sm border border-amber-900/40 rounded-lg p-4 hover:border-amber-700/60 transition-all duration-300 transform hover:scale-[1.02] hover:shadow-lg">
          <div class="flex items-center space-x-4">
            <div class="flex-1 min-w-0">
              <div class="flex gap-2">
                <div class="w-8 h-8 self-center">
                  <img 
                    src={`/images/avatars/class_${playerData.class || 'default'}.jpg`}
                    alt={playerData.class || 'Clase'}
                    class="w-full h-full rounded-full border-2 border-amber-600/50 object-cover"
                    onError="this.src='/images/avatars/default.png'"
                  />
                </div>
                <h3 class="text-lg font-semibold text-amber-100 truncate" style={`color: #${classData.color}`}>
                  {playerName}
                </h3>
              </div>
              {playerData.rank && (
                <div class="bg-amber-700 text-amber-100 text-xs inline-block font-bold px-2 py-0.5 rounded-full border border-amber-600/50 mt-1">
                  {playerData.rank}
                </div>
              )}
              <p class="text-sm text-amber-100/80 mt-2">
                {recognitionText}
              </p>
              
              {player.recognitionIndices?.length > 0 && (
                <div class="mt-2 space-y-1">
                  {player.recognitionIndices.map((idx: number) => {
                    const recog = recognitions[idx];
                    if (!recog) return null;
                    return (
                      <div class="text-xs text-amber-100/70 flex items-start gap-1">
                        <span class="text-amber-300/80">•</span>
                        <span>{recog.description}</span>
                      </div>
                    );
                  })}
                </div>
              )}
            </div>
          </div>
        </div>
      );
    })}
  </div>
  
  {totalPages > 1 && (
    <div id="featured-players-pagination" class="mt-6">
      <!-- Los controles de paginación se generarán con JavaScript -->
    </div>
  )}
  
  <!-- Controles de paginación -->
  {totalPages > 1 && (
    <div class="flex items-center justify-center gap-2 mt-6">
      <a 
        href={currentPage > 1 ? `?page=${currentPage - 1}` : '#'}
        class={`px-3 py-1 rounded-md ${
          currentPage === 1 
            ? 'bg-gray-700 text-gray-500 cursor-not-allowed' 
            : 'bg-amber-700/50 hover:bg-amber-600/60 text-amber-100'
        }`}
        aria-disabled={currentPage === 1}
      >
        Anterior
      </a>
      
      <div class="flex items-center gap-1">
        {Array.from({ length: Math.min(5, totalPages) }, (_, i) => {
          const pageNum = Math.max(1, Math.min(totalPages - 4, currentPage - 2)) + i;
          if (pageNum < 1 || pageNum > totalPages) return null;
          
          return (
            <a 
              href={`?page=${pageNum}`}
              class={`w-8 h-8 flex items-center justify-center rounded-md ${
                pageNum === currentPage 
                  ? 'bg-amber-600 text-white' 
                  : 'bg-gray-700 hover:bg-gray-600 text-gray-200'
              }`}
            >
              {pageNum}
            </a>
          );
        })}
      </div>
      
      <a 
        href={currentPage < totalPages ? `?page=${currentPage + 1}` : '#'}
        class={`px-3 py-1 rounded-md ${
          currentPage === totalPages 
            ? 'bg-gray-700 text-gray-500 cursor-not-allowed' 
            : 'bg-amber-700/50 hover:bg-amber-600/60 text-amber-100'
        }`}
        aria-disabled={currentPage === totalPages}
      >
        Siguiente
      </a>
    </div>
  )}
  
  <script is:inline>
    // Manejar la navegación con JavaScript cuando esté disponible
    document.addEventListener('DOMContentLoaded', function() {
      // Función para manejar los clics en los enlaces de paginación
      function handlePaginationClick(event) {
        const link = event.target.closest('a');
        if (!link || link.getAttribute('aria-disabled') === 'true') {
          event.preventDefault();
          return;
        }
        
        // Obtener el número de página del href
        const url = new URL(link.href);
        const page = parseInt(url.searchParams.get('page') || '1');
        
        // Actualizar la URL sin recargar la página
        if (window.history.pushState) {
          window.history.pushState({}, '', url);
          // Forzar recarga para ver los cambios
          window.location.reload();
        }
      }
      
      // Agregar manejadores de eventos a los enlaces de paginación
      document.querySelectorAll('#featured-players-pagination a').forEach(link => {
        link.addEventListener('click', handlePaginationClick);
      });
    });
  </script>
</div>

<style>
  .featured-player {
    transition: all 0.3s ease;
  }
  .featured-player:hover {
    transform: translateY(-2px);
  }
</style>
