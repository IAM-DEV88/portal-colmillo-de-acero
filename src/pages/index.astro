---
import Layout from '../components/layout/Layout.astro';
---

<Layout title="Inicio">
  <!-- Main Container -->
  <main class="overflow-x-hidden min-h-screen w-full">
    <!-- Main Content -->
    <div class="relative flex flex-col items-center justify-center py-12 px-4 sm:px-6 lg:px-8 w-full max-w-full mx-auto">
      <!-- Header -->
      <header class="text-center mb-12 animate-fade-in">
        <h1 class="text-3xl md:text-4xl font-bold mb-4 text-transparent bg-clip-text bg-gradient-to-r from-amber-400 via-amber-300 to-amber-500 font-title tracking-wider">
          BIENVENIDOS
        </h1>
        <div class="w-48 h-0.5 bg-gradient-to-r from-amber-900/0 via-amber-500/80 to-amber-900/0 mx-auto my-6"></div>
      </header>

      <!-- Navigation Cards -->
      <div class="grid grid-cols-1 md:grid-cols-3 gap-6 w-full max-w-6xl">
        <!-- Roster Card -->
        <a 
          href="/roster" 
          class="relative group bg-gray-900/60 backdrop-blur-sm border border-amber-900/30 rounded-xl p-6 transition-all duration-300 hover:border-amber-800/50 hover:shadow-lg hover:shadow-amber-500/5 hover:-translate-y-1 overflow-hidden animate-fade-in opacity-0"
          style="animation-delay: 0.2s;"
        >
          <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
          <div class="relative z-10">
            <div class="text-amber-400 mb-4 text-3xl">
              <i class="fas fa-users"></i>
            </div>
            <h3 class="text-xl font-bold text-white mb-2">ROSTER</h3>
            <p class="text-amber-100/70 text-sm mb-4">Conoce a los miembros de nuestra hermandad y sus roles</p>
            <div class="mt-4 text-xs text-amber-400 flex items-center group-hover:text-amber-300 transition-colors">
              <span>VER MÁS</span>
              <i class="fas fa-arrow-right ml-2 transition-transform group-hover:translate-x-1"></i>
            </div>
          </div>
          <div class="absolute inset-0 border-2 border-transparent group-hover:border-amber-500/20 rounded-xl transition-all duration-300 pointer-events-none"></div>
        </a>

        <!-- Raids Card -->
        <a 
          href="/raids" 
          class="relative group bg-gray-900/60 backdrop-blur-sm border border-amber-900/30 rounded-xl p-6 transition-all duration-300 hover:border-amber-800/50 hover:shadow-lg hover:shadow-amber-500/5 hover:-translate-y-1 overflow-hidden animate-fade-in opacity-0"
          style="animation-delay: 0.4s;"
        >
          <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
          <div class="relative z-10">
            <div class="text-amber-400 mb-4 text-3xl">
              <i class="fas fa-dungeon"></i>
            </div>
            <h3 class="text-xl font-bold text-white mb-2">RAIDS</h3>
            <p class="text-amber-100/70 text-sm mb-4">Horarios, líderes y progreso de incursiones</p>
            <div class="mt-4 text-xs text-amber-400 flex items-center group-hover:text-amber-300 transition-colors">
              <span>VER MÁS</span>
              <i class="fas fa-arrow-right ml-2 transition-transform group-hover:translate-x-1"></i>
            </div>
          </div>
          <div class="absolute inset-0 border-2 border-transparent group-hover:border-amber-500/20 rounded-xl transition-all duration-300 pointer-events-none"></div>
        </a>

        <!-- Guides Card -->
        <a 
          href="/guides" 
          class="relative group bg-gray-900/60 backdrop-blur-sm border border-amber-900/30 rounded-xl p-6 transition-all duration-300 hover:border-amber-800/50 hover:shadow-lg hover:shadow-amber-500/5 hover:-translate-y-1 overflow-hidden animate-fade-in opacity-0"
          style="animation-delay: 0.6s;"
        >
          <div class="absolute inset-0 bg-grid-pattern opacity-5"></div>
          <div class="relative z-10">
            <div class="text-amber-400 mb-4 text-3xl">
              <i class="fas fa-book"></i>
            </div>
            <h3 class="text-xl font-bold text-white mb-2">GUÍAS</h3>
            <p class="text-amber-100/70 text-sm mb-4">Estrategias y consejos para el contenido de endgame</p>
            <div class="mt-4 text-xs text-amber-400 flex items-center group-hover:text-amber-300 transition-colors">
              <span>VER MÁS</span>
              <i class="fas fa-arrow-right ml-2 transition-transform group-hover:translate-x-1"></i>
            </div>
          </div>
          <div class="absolute inset-0 border-2 border-transparent group-hover:border-amber-500/20 rounded-xl transition-all duration-300 pointer-events-none"></div>
        </a>
      </div>
    </div>
  </main>
  
  <style>
    /* Grid Pattern Background */
    .bg-grid-pattern {
      background-image: 
        linear-gradient(to right, currentColor 1px, transparent 1px),
        linear-gradient(to bottom, currentColor 1px, transparent 1px);
      background-size: 40px 40px;
      color: rgba(255, 255, 255, 0.05);
    }
    
    /* Font Awesome Icons */
    .fa-users, .fa-dungeon, .fa-book, .fa-arrow-right, 
    .fa-discord, .fa-twitter, .fa-youtube {
      font-family: 'Font Awesome 6 Free';
      font-weight: 900;
    }
    
    /* Animation for the header underline */
    @keyframes gradient {
      0% { background-position: 0% 50%; }
      50% { background-position: 100% 50%; }
      100% { background-position: 0% 50%; }
    }
    
    /* Custom font for the title */
    .font-title {
      font-family: 'Arial', sans-serif;
      letter-spacing: 2px;
      text-transform: uppercase;
    }
  </style>

<!-- AOS Animation Library -->
<script src="https://unpkg.com/aos@next/dist/aos.js"></script>
<script>
  document.addEventListener('DOMContentLoaded', () => {
    // @ts-ignore - AOS is loaded from CDN
    if (window.AOS) {
      // @ts-ignore
      AOS.init({
        duration: 800,
        once: true,
        easing: 'ease-out-quart',
      });
    }
  });
</script>
