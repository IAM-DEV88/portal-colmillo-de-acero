---
import Layout from '../components/layout/Layout.astro';
import RosterTable from '../components/roster/RosterTable.astro';
import FeaturedPlayers from '../components/roster/FeaturedPlayers.astro';
import rosterData from '../data/roster.json';
import { validatePublicNote } from '../utils/rosterUtils';

// Definir el tipo para los miembros del roster
interface RosterMember {
  name: string;
  officerNote?: string;
  class: string;
  publicNote?: string;
  rank?: string;
  noteValidation?: any;
}

// Importar tipos y datos
import type { FeaturedPlayersData, FeaturedPlayer, Recognition } from '../types/featuredPlayers';
import featuredDataRaw from '../data/featuredPlayers.json';

// Hacer un type assertion para los datos
const featuredData = featuredDataRaw as unknown as FeaturedPlayersData;
const allRecognitionsBase = featuredData.recognitions || [];

// Mapear todos los reconocimientos por jugador a través del tiempo
const playerAllRecognitions: Record<string, any[]> = {};

Object.entries(featuredData).forEach(([year, yearData]) => {
  if (year === 'recognitions') return;
  
  Object.entries(yearData as Record<string, any>).forEach(([month, monthData]) => {
    if (!monthData.featuredPlayers) return;
    
    monthData.featuredPlayers.forEach((player: FeaturedPlayer) => {
      if (!player.playerName) return;
      const name = player.playerName.toLowerCase();
      if (!playerAllRecognitions[name]) {
        playerAllRecognitions[name] = [];
      }
      
      const playerMonthRecs = (player.recognitionIndices || []).map(idx => {
        const baseRec = allRecognitionsBase[idx];
        if (!baseRec) return null;
        return {
          ...baseRec,
          date: `${year}-${month}-01`, // Asignar una fecha para el modal
          index: idx
        };
      }).filter(Boolean);
      
      playerAllRecognitions[name].push(...playerMonthRecs);
    });
  });
});

// Asegurarse de que los datos tengan la estructura esperada
const players = (rosterData as any).players || {};
const formattedRosterData = Object.entries(players).map(([name, memberData]: [string, any]) => {
  const member = { ...memberData, name }; // Asegurar que el nombre esté incluido
  const lowerName = name.toLowerCase();
  const playerRecognitions = playerAllRecognitions[lowerName] || [];

  try {
    // Validar la nota pública si existe
    const publicNote = String(member.publicNote || '').trim();
    const noteValidation = validatePublicNote(publicNote, member.name || 'Desconocido');

    // Asegurar que la clase sea válida
    const className = String(member.class || 'Desconocido').trim();

    return {
      ...member,
      name: String(name || 'Sin nombre').trim(),
      class: className,
      rank: String(member.rank || 'Miembro').trim(),
      publicNote,
      noteValidation,
      officerNote: String(member.officerNote || '').trim(),
      mainAlt: member.mainAlt || 'A',
      level: Number(member.level) || 80,
      gearScore: Number(member.gearScore) || 0,
      recognitions: playerRecognitions,
      // Asegurar que los campos de validación estén presentes
      validation: {
        ...noteValidation,
        isValid: noteValidation.isValid || false,
        missingFields: Array.isArray(noteValidation.missingFields)
          ? noteValidation.missingFields
          : [],
      },
    };
  } catch (error) {
    console.error('Error procesando miembro:', member, error);
    return {
      ...member,
      name: String(member.name || 'Error en datos').trim(),
      class: 'Desconocido',
      rank: 'Error',
      publicNote: '',
      noteValidation: {
        isValid: false,
        errors: ['Error al procesar datos del personaje'],
        missingFields: ['Datos inválidos'],
      },
      officerNote: '',
      mainAlt: 'M',
      level: 0,
      gearScore: 0,
      validation: {
        isValid: false,
        errors: ['Error al procesar datos del personaje'],
        missingFields: ['Datos inválidos'],
      },
    };
  }
});

// Obtener el año y mes actual
const currentYear = new Date().getFullYear().toString();
const currentMonth = (new Date().getMonth() + 1).toString().padStart(2, '0');

// Obtener jugadores destacados del mes actual
const currentYearData = featuredData[currentYear];
const currentMonthData =
  currentYearData && currentMonth in (currentYearData as any)
    ? (currentYearData as any)[currentMonth].featuredPlayers
    : [];

const allRecognitions = featuredData.recognitions || [];

// Filtrar jugadores que existen en el roster y revertir el orden
const featuredPlayers = currentMonthData
  .filter(
    (player: FeaturedPlayer) =>
      !player.playerName ||
      formattedRosterData.some(
        (member) => member.name.toLowerCase() === player.playerName.toLowerCase()
      )
  )
  .reverse(); // Invertir el orden para mostrar los más recientes primero

// Manejar la paginación - 3 en móvil, 10 en pantallas más grandes
let itemsPerPage = 3; // Valor por defecto para móvil
let totalPages = 1; // Inicializar variable

// Función para actualizar items por página según el ancho de pantalla
function updateItemsPerPage() {
  itemsPerPage = window.innerWidth < 768 ? 3 : 9;
  totalPages = Math.ceil(featuredPlayers.length / itemsPerPage);
  // Asegurarse de que la página actual no exceda el número total de páginas
  if (currentPage > totalPages && totalPages > 0) {
    currentPage = totalPages;
  }
}

// Inicializar con el valor correcto
if (typeof window !== 'undefined') {
  updateItemsPerPage();
  // Actualizar al cambiar el tamaño de la ventana
  window.addEventListener('resize', updateItemsPerPage);
}

// Obtener la página actual de la URL
let currentPage = 1;
if (Astro.url.searchParams.has('page')) {
  const page = parseInt(Astro.url.searchParams.get('page') || '1');
  if (!isNaN(page) && page >= 1 && page <= totalPages) {
    currentPage = page;
  }
}
---

<Layout title="Roster">
  <div class="mx-auto animate-fade-in max-w-7xl px-4 sm:px-6 lg:px-8 py-12 space-y-16">
    <!-- Header de la Página -->
    <header class="text-center space-y-4 mb-16 relative">
      <div class="absolute inset-0 -top-24 -z-10 bg-[radial-gradient(circle_at_center,rgba(217,119,6,0.1)_0%,transparent_70%)] blur-3xl"></div>
      
      <div class="inline-flex items-center space-x-2 px-3 py-1 rounded-full bg-amber-500/10 border border-amber-500/20 text-amber-400 text-[10px] font-black uppercase tracking-[0.2em] mb-4">
        <span class="relative flex h-2 w-2">
          <span class="animate-ping absolute inline-flex h-full w-full rounded-full bg-amber-400 opacity-75"></span>
          <span class="relative inline-flex rounded-full h-2 w-2 bg-amber-500"></span>
        </span>
        <span>Comunidad Activa</span>
      </div>
      
      <h1 class="text-4xl sm:text-6xl font-black text-white uppercase tracking-tighter italic">
        Portal de <span class="text-transparent bg-clip-text bg-gradient-to-r from-amber-200 via-amber-400 to-amber-200">Hermandad</span>
      </h1>
      
      <p class="text-gray-400 max-w-2xl mx-auto text-sm sm:text-base leading-relaxed font-medium">
        Seguimiento detallado de nuestros miembros, progresos y reconocimientos. 
        Mantente al día con las reglas y la excelencia de nuestra comunidad.
      </p>
      
      <div class="flex items-center justify-center gap-4 pt-4">
        <div class="h-px w-12 bg-gradient-to-r from-transparent to-amber-500/50"></div>
        <div class="w-2 h-2 rounded-full bg-amber-500/20 border border-amber-500/50"></div>
        <div class="h-px w-12 bg-gradient-to-l from-transparent to-amber-500/50"></div>
      </div>
    </header>

    <!-- Sección de Jugadores Destacados -->
    <section class="relative">
      <FeaturedPlayers
        featuredPlayers={featuredPlayers}
        rosterData={formattedRosterData}
        recognitions={allRecognitions}
        currentPage={currentPage}
        totalPages={totalPages}
      />
    </section>

    <!-- Tabla de Roster -->
    <section class="relative space-y-6">
      <div class="flex flex-col md:flex-row md:justify-between md:items-end gap-4 px-2">
        <div class="flex items-center space-x-4">
          <div class="w-12 h-12 bg-gradient-to-br from-amber-600 to-amber-800 rounded-xl flex items-center justify-center shadow-lg shadow-amber-900/20 transform -rotate-2 hover:rotate-0 transition-transform duration-300">
            <svg class="w-6 h-6 text-white" fill="none" stroke="currentColor" viewBox="0 0 24 24">
              <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"></path>
            </svg>
          </div>
          <div>
            <h2 class="text-2xl sm:text-3xl font-black text-white uppercase tracking-tighter italic">
              Roster de <span class="text-transparent bg-clip-text bg-gradient-to-r from-amber-200 via-amber-400 to-amber-200">Miembros</span>
            </h2>
            <p class="text-amber-300/60 font-medium tracking-widest uppercase text-[10px] mt-0.5">Listado completo y estadísticas de la hermandad</p>
          </div>
        </div>
      </div>
      <RosterTable initialRosterData={formattedRosterData} />
    </section>

    <!-- Reglas de Hermandad -->
    <section class="relative">
      <div
        class="bg-gradient-to-br from-amber-900/10 via-gray-900/40 to-amber-800/5 backdrop-blur-md border border-amber-600/20 rounded-3xl p-6 sm:p-10 hover:border-amber-500/40 transition-all duration-500 hover:shadow-2xl hover:shadow-amber-900/20 relative overflow-hidden"
      >
        <!-- Decorative background elements -->
        <div class="absolute -top-24 -right-24 w-64 h-64 bg-amber-600/10 rounded-full blur-3xl"></div>
        <div class="absolute -bottom-24 -left-24 w-64 h-64 bg-amber-700/5 rounded-full blur-3xl"></div>
        <div class="flex items-center justify-center mb-10">
          <div class="flex flex-col items-center space-y-3">
            <div class="w-16 h-16 bg-gradient-to-br from-amber-500 to-amber-700 rounded-2xl flex items-center justify-center shadow-xl shadow-amber-900/30 group">
              <svg class="w-8 h-8 text-white group-hover:scale-110 transition-transform duration-300" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                <path
                  stroke-linecap="round"
                  stroke-linejoin="round"
                  stroke-width="2"
                  d="M9 12l2 2 4-4m5.618-4.016A11.955 11.955 0 0112 2.944a11.955 11.955 0 01-8.618 3.04A12.02 12.02 0 003 9c0 5.591 3.824 10.29 9 11.622 5.176-1.332 9-6.03 9-11.622 0-1.042-.133-2.052-.382-3.016z"
                ></path>
              </svg>
            </div>
            <h2 class="text-3xl sm:text-4xl font-black text-white uppercase tracking-tighter text-center italic">
              Reglas de <span class="text-transparent bg-clip-text bg-gradient-to-r from-amber-200 via-amber-400 to-amber-200">Hermandad</span>
            </h2>
            <div class="w-24 h-1 bg-gradient-to-r from-transparent via-amber-500 to-transparent rounded-full opacity-50"></div>
          </div>
        </div>
        
        <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
      <!-- Norma 1 -->
      <div class="group bg-gray-900/40 backdrop-blur-md border border-amber-900/30 rounded-2xl p-5 hover:bg-gray-800/60 hover:border-amber-500/40 transition-all duration-300 hover:shadow-xl hover:shadow-amber-900/10 relative overflow-hidden">
        <div class="absolute -right-8 -top-8 w-24 h-24 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/10 transition-colors duration-500"></div>
        <div class="flex items-start space-x-4 relative z-10">
          <div
            class="w-10 h-10 bg-gradient-to-br from-amber-500 to-amber-700 rounded-xl flex-shrink-0 flex items-center justify-center shadow-lg shadow-amber-900/20 group-hover:scale-110 transition-transform duration-300"
          >
            <span class="text-white text-lg font-black italic">1</span>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-center space-x-2 mb-2">
              <div class="p-1.5 bg-amber-500/10 rounded-lg">
                <svg
                  class="w-4 h-4 text-amber-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M9 12h6m-6 4h6m2 5H7a2 2 0 01-2-2V5a2 2 0 012-2h5.586a1 1 0 01.707.293l5.414 5.414a1 1 0 01.293.707V19a2 2 0 01-2 2z"
                  ></path>
                </svg>
              </div>
              <h4 class="font-bold text-amber-400 text-sm uppercase tracking-wider">Código de Nota</h4>
            </div>
            <p class="text-gray-200 text-sm leading-relaxed mb-2 font-medium">
              WISP función y gear score al General Admin u Oficial en línea por wisp para subir de rango.
            </p>
            <div class="flex items-start space-x-2">
              <div class="w-1.5 h-1.5 bg-amber-500/40 rounded-full mt-1.5 flex-shrink-0"></div>
              <p class="text-gray-400 text-xs italic leading-tight">
                Asi garantizan su participación en raids y sorteos.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Norma 2 -->
      <div class="group bg-gray-900/40 backdrop-blur-md border border-amber-900/30 rounded-2xl p-5 hover:bg-gray-800/60 hover:border-amber-500/40 transition-all duration-300 hover:shadow-xl hover:shadow-amber-900/10 relative overflow-hidden">
        <div class="absolute -right-8 -top-8 w-24 h-24 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/10 transition-colors duration-500"></div>
        <div class="flex items-start space-x-4 relative z-10">
          <div
            class="w-10 h-10 bg-gradient-to-br from-amber-500 to-amber-700 rounded-xl flex-shrink-0 flex items-center justify-center shadow-lg shadow-amber-900/20 group-hover:scale-110 transition-transform duration-300"
          >
            <span class="text-white text-lg font-black italic">2</span>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-center space-x-2 mb-2">
              <div class="p-1.5 bg-amber-500/10 rounded-lg">
                <svg
                  class="w-4 h-4 text-amber-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M20 7l-8-4-8 4m16 0l-8 4m8-4v10l-8 4m0-10L4 7m8 4v10M4 7v10l8 4"></path>
                </svg>
              </div>
              <h4 class="font-bold text-amber-400 text-sm uppercase tracking-wider">Baul de Sorteos</h4>
            </div>
            <p class="text-gray-200 text-sm leading-relaxed mb-2 font-medium">
              Se realizan sorteos aleatorios para recompensar a los jugadores en línea.
            </p>
            <div class="flex items-start space-x-2">
              <div class="w-1.5 h-1.5 bg-amber-500/40 rounded-full mt-1.5 flex-shrink-0"></div>
              <p class="text-gray-400 text-xs italic leading-tight">
                Se reciben donaciones para mantener el baul funcionando.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Norma 3 -->
      <div class="group bg-gray-900/40 backdrop-blur-md border border-amber-900/30 rounded-2xl p-5 hover:bg-gray-800/60 hover:border-amber-500/40 transition-all duration-300 hover:shadow-xl hover:shadow-amber-900/10 relative overflow-hidden">
        <div class="absolute -right-8 -top-8 w-24 h-24 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/10 transition-colors duration-500"></div>
        <div class="flex items-start space-x-4 relative z-10">
          <div
            class="w-10 h-10 bg-gradient-to-br from-amber-500 to-amber-700 rounded-xl flex-shrink-0 flex items-center justify-center shadow-lg shadow-amber-900/20 group-hover:scale-110 transition-transform duration-300"
          >
            <span class="text-white text-lg font-black italic">3</span>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-center space-x-2 mb-2">
              <div class="p-1.5 bg-amber-500/10 rounded-lg">
                <svg
                  class="w-4 h-4 text-amber-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M19 11H5m14 0a2 2 0 012 2v6a2 2 0 01-2 2H5a2 2 0 01-2-2v-6a2 2 0 012-2m14 0V9a2 2 0 00-2-2M5 11V9a2 2 0 012-2m0 0V5a2 2 0 012-2h6a2 2 0 012 2v2M7 7h10"
                  ></path>
                </svg>
              </div>
              <h4 class="font-bold text-amber-400 text-sm uppercase tracking-wider">Farm lvl 74+</h4>
            </div>
            <p class="text-gray-200 text-sm leading-relaxed mb-2 font-medium">
              Materiales e items lvl 74+ como orbes congelados, fragmento onirico, consumibles, etc.
            </p>
            <div class="flex items-start space-x-2">
              <div class="w-1.5 h-1.5 bg-amber-500/40 rounded-full mt-1.5 flex-shrink-0"></div>
              <p class="text-gray-400 text-xs italic leading-tight">
                Se reciben donaciones para mantener el baul funcionando.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Norma 4 -->
      <div class="group bg-gray-900/40 backdrop-blur-md border border-amber-900/30 rounded-2xl p-5 hover:bg-gray-800/60 hover:border-amber-500/40 transition-all duration-300 hover:shadow-xl hover:shadow-amber-900/10 relative overflow-hidden">
        <div class="absolute -right-8 -top-8 w-24 h-24 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/10 transition-colors duration-500"></div>
        <div class="flex items-start space-x-4 relative z-10">
          <div
            class="w-10 h-10 bg-gradient-to-br from-amber-500 to-amber-700 rounded-xl flex-shrink-0 flex items-center justify-center shadow-lg shadow-amber-900/20 group-hover:scale-110 transition-transform duration-300"
          >
            <span class="text-white text-lg font-black italic">4</span>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-center space-x-2 mb-2">
              <div class="p-1.5 bg-amber-500/10 rounded-lg">
                <svg
                  class="w-4 h-4 text-amber-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M5 3v4M3 5h4M6 17v4m-2-2h4m5-16l2.286 6.857L21 12l-5.714 2.143L13 21l-2.286-6.857L5 12l5.714-2.143L13 3z"
                  ></path>
                </svg>
              </div>
              <h4 class="font-bold text-amber-400 text-sm uppercase tracking-wider">Reconocimientos</h4>
            </div>
            <p class="text-gray-200 text-sm leading-relaxed mb-2 font-medium">
              Los jugadores que contribuyen activamente al flujo de la hermandad son reconocidos
              mensualmente.
            </p>
            <div class="flex items-start space-x-2">
              <div class="w-1.5 h-1.5 bg-amber-500/40 rounded-full mt-1.5 flex-shrink-0"></div>
              <p class="text-gray-400 text-xs italic leading-tight">
                Se anima a todos los miembros a liderar actividades y eventos.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Norma 5 - Trabajo en Equipo -->
      <div class="group bg-gray-900/40 backdrop-blur-md border border-amber-900/30 rounded-2xl p-5 hover:bg-gray-800/60 hover:border-amber-500/40 transition-all duration-300 hover:shadow-xl hover:shadow-amber-900/10 relative overflow-hidden">
        <div class="absolute -right-8 -top-8 w-24 h-24 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/10 transition-colors duration-500"></div>
        <div class="flex items-start space-x-4 relative z-10">
          <div
            class="w-10 h-10 bg-gradient-to-br from-amber-500 to-amber-700 rounded-xl flex-shrink-0 flex items-center justify-center shadow-lg shadow-amber-900/20 group-hover:scale-110 transition-transform duration-300"
          >
            <span class="text-white text-lg font-black italic">5</span>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-center space-x-2 mb-2">
              <div class="p-1.5 bg-amber-500/10 rounded-lg">
                <svg
                  class="w-4 h-4 text-amber-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M17 20h5v-2a3 3 0 00-5.356-1.857M17 20H7m10 0v-2c0-.656-.126-1.283-.356-1.857M7 20H2v-2a3 3 0 015.356-1.857M7 20v-2c0-.656.126-1.283.356-1.857m0 0a5.002 5.002 0 019.288 0M15 7a3 3 0 11-6 0 3 3 0 016 0zm6 3a2 2 0 11-4 0 2 2 0 014 0zM7 10a2 2 0 11-4 0 2 2 0 014 0z"
                  ></path>
                </svg>
              </div>
              <h4 class="font-bold text-amber-400 text-sm uppercase tracking-wider">Baul de Equipamiento</h4>
            </div>
            <p class="text-gray-200 text-sm leading-relaxed mb-2 font-medium">
              Acceso instantáneo a los elementos del baul de equipamiento.
            </p>
            <div class="flex items-start space-x-2">
              <div class="w-1.5 h-1.5 bg-amber-500/40 rounded-full mt-1.5 flex-shrink-0"></div>
              <p class="text-gray-400 text-xs italic leading-tight">
                Solicitar al GM en Discord <a class="text-amber-400/80 hover:text-amber-300 hover:underline transition-colors" href="https://discord.gg/kMK2ZRRCza">kMK2ZRRCza</a>.
              </p>
            </div>
          </div>
        </div>
      </div>

      <!-- Norma 6 - Respeto -->
      <div class="group bg-gray-900/40 backdrop-blur-md border border-amber-900/30 rounded-2xl p-5 hover:bg-gray-800/60 hover:border-amber-500/40 transition-all duration-300 hover:shadow-xl hover:shadow-amber-900/10 relative overflow-hidden">
        <div class="absolute -right-8 -top-8 w-24 h-24 bg-amber-500/5 rounded-full blur-2xl group-hover:bg-amber-500/10 transition-colors duration-500"></div>
        <div class="flex items-start space-x-4 relative z-10">
          <div
            class="w-10 h-10 bg-gradient-to-br from-amber-500 to-amber-700 rounded-xl flex-shrink-0 flex items-center justify-center shadow-lg shadow-amber-900/20 group-hover:scale-110 transition-transform duration-300"
          >
            <span class="text-white text-lg font-black italic">6</span>
          </div>
          <div class="flex-1 min-w-0">
            <div class="flex items-center space-x-2 mb-2">
              <div class="p-1.5 bg-amber-500/10 rounded-lg">
                <svg
                  class="w-4 h-4 text-amber-400"
                  fill="none"
                  stroke="currentColor"
                  viewBox="0 0 24 24"
                >
                  <path
                    stroke-linecap="round"
                    stroke-linejoin="round"
                    stroke-width="2"
                    d="M14.828 14.828a4 4 0 01-5.656 0M9 10h.01M15 10h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"
                  ></path>
                </svg>
              </div>
              <h4 class="font-bold text-amber-400 text-sm uppercase tracking-wider">Respeto</h4>
            </div>
            <p class="text-gray-200 text-sm leading-relaxed mb-2 font-medium">
              Respeto y cooperación entre todos los miembros. No se tolerará discriminación o acoso.
            </p>
            <div class="flex items-start space-x-2">
              <div class="w-1.5 h-1.5 bg-amber-500/40 rounded-full mt-1.5 flex-shrink-0"></div>
              <p class="text-gray-400 text-xs italic leading-tight">
                Mantener un ambiente sano y de respeto mutuo.
              </p>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>
</div>
  </div></Layout
>
